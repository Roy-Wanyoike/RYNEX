# RYNEX Vehicles API — environment template
# Copy to .env and fill in. NEVER commit the real .env.

# --- Server ---
PORT=4000
CORS_ORIGIN=*

# --- Database (SQL Server) ---
DB_SERVER=localhost
DB_NAME=carshop
DB_USER=sa
DB_PWD=change_me_strong_password
DB_ENCRYPT=false

# --- Auth ---
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SECRETKEY=change_me_long_random_secret
